---
id: TASK-0070
title: "Scope picker UX + agent KM overview matrix"
status: completed
sprint: SPRINT-015
points: 3
owner: dev
updated: 2026-07-12
---

# TASK-0070: Scope UX Polish

## Outcome

Clear scope assignment and at-a-glance KM health per agent.

## Acceptance Criteria

1. Scope dropdown on upload uses `GET /api/tenant/km/scopes` (not hard-coded only).
2. Per-document **change scope** action → `PATCH` (disabled while processing if status not ready).
3. Overview panel/matrix: for each agent show doc count by scope (or total + scopes present).
4. Empty state when agent has zero documents (CTA to upload).
5. Error states: 403 inactive tenant, 429 quota — user-readable messages.
6. Optional: show read-only “default retrieval scopes for this agent” help text from known S2 map (ava→general, etc.) without editing the hard-map.

## Verification

```bash
# UI: change scope on existing doc; overview counts update after upload/delete
```

## Depends

- TASK-0068, TASK-0069
