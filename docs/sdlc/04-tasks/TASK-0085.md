---
id: TASK-0085
title: "Apply tier overrides on preview + settings link"
status: completed
sprint: SPRINT-018
feature: FEAT-0020
points: 3
owner: dev
updated: 2026-07-12
---

# TASK-0085: Tier Apply + Settings

## Outcome

Preview (and voice/chat when `tier_id` provided) honor tier locale and call-cap overrides; settings points to Tiers admin.

## Acceptance Criteria

1. Preview UI: optional **Tier** select (load from GET tiers).  
2. `POST /api/tenant/preview/chat` accepts `tier_id`; applies `ai_reply_locale` if set.  
3. Preview voice / package voice path: when `tier_id` query set, use tier call caps if > 0 else tenant caps.  
4. Settings page: help text links to `/tenant/tiers`; deprecate free-text tier/group as primary UX (fields may remain for notes).  
5. Document future: S19 customers.tier_id.

## Verification

```bash
# Create VIP with ai_reply_locale=th
# Preview chat with tier_id → Thai preference in replies
```
