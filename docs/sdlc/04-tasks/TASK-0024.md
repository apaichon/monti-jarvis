---
id: TASK-0024
title: "Platform admin portal — avatars + tenant assignment UI"
status: completed
sprint: SPRINT-005
points: 3
owner: dev
updated: 2026-07-09
---

# TASK-0024: Platform Admin Portal — Avatars + Tenant Assignment UI

## Outcome

Extend `apps/platform-admin-web` with avatars management and per-tenant assignment screens (P7+ wireframes from sprint-tech-specs).

## Acceptance Criteria

1. Nav link **Avatars** in app shell; routes `/admin/avatars`, `/admin/avatars/new`, `/admin/avatars/[id]`.
2. List page: table slug, name, role, status; edit + archive actions.
3. Create/edit form: slug, name, role, trait, color, image URL, greeting, status; **voice profiles** table (provider, voice_id, voice, priority, status) per P8/P9 wireframes.
4. Tenant assignment: `/admin/tenants/[id]/avatars` — current assignments, assign from catalog dropdown, revoke; show entitlement cap hint.
5. API errors surfaced (401 redirect login, 409 cap message); `make platform-admin-web` included in `make build`.

## Verification

```bash
make build && open http://localhost:8091/admin/avatars
```

# UAT path in docs/sdlc/06-manual-tests/SPRINT-005-manual.md (Tester)