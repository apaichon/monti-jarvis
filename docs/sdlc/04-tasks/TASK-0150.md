---
id: TASK-0150
title: "Apply branding + theme CSS on customer desk and embed"
status: completed
sprint: SPRINT-039
feature: FEAT-0035
design: DES-0037
points: 4
owner: dev
updated: 2026-07-19
---

# TASK-0150: Apply branding + colors (customer + embed)

## Outcome

Published theme drives embed/customer **header** (logo, brand name, subtitle) and **chrome colors** (panels, Start call, Send, borders, text) via CSS variables.

## Acceptance criteria

1. Embed header no longer hard-codes only Monti mark when published branding exists — uses `logo_url`, `brand_name`, `subtitle`.
2. Customer desk brand header applies the same published branding.
3. CSS variables from DES-0037 map to buttons, surfaces, lines, text, waveform accent.
4. Fallback chain: empty fields → Monti defaults / workspace name / default subtitle / dark palette.
5. Public theme + embed resolve expose `branding` + `tokens`; draft never public.
6. Tenant isolation: Tenant B cannot see Tenant A branding/colors.

## Notes

Primary UI reference: embed caller chrome screenshot (Libra-style header + Start call + Send).
