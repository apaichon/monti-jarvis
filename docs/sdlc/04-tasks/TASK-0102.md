---
id: TASK-0102
title: "Conversation archive schema and MinIO object contract"
status: completed
sprint: SPRINT-022
feature: FEAT-0024
points: 4
owner: devops
updated: 2026-07-13
---

# TASK-0102: Conversation Archive Schema and Object Contract

## Outcome

Postgres and MinIO contracts can track tenant-scoped conversation records, archive objects, archive status, and archive protection mode.

## Acceptance criteria

1. Add tenant-scoped conversation record/archive metadata tables or extend existing call tables safely.
2. Define deterministic MinIO object paths under `calls/{tenant_id}/{call_id}/...`.
3. Store archive status, content type, checksum/size where useful, and safe error/retry metadata.
4. Support a documented archive protection/encryption mode without leaking secrets into metadata.
5. Migration/bootstrap is idempotent and covered by Go tests.

## Verification

```bash
go test ./...
```

## Review — PASS

- Conversation record, archive object, customer usage event schema, and MinIO object contract are implemented with tenant-scoped metadata.
- Verified with `/usr/local/go/bin/go test ./...`.
