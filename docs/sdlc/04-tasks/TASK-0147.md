---
id: TASK-0147
title: "Framework packages: Vue, React, Svelte, Web Component"
status: completed
sprint: SPRINT-037
feature: FEAT-0017
points: 6
owner: dev
updated: 2026-07-18
---

# TASK-0147: Framework embed packages

## Outcome

Publishable monorepo packages wrapping `@monti/embed-core` for Vue 3, React, Svelte, and `<monti-embed>` custom element.

## Acceptance criteria

1. `@monti/embed-vue` — component + plugin; open/close/error events.
2. `@monti/embed-react` — component + `useMontiEmbed` hook; unmount destroys instance.
3. `@monti/embed-svelte` — Svelte component + imperative `mountMontiEmbedSvelte`.
4. `@monti/embed-web-component` — auto-registered `<monti-embed>` with attribute API and `monti-*` events.
5. All packages typecheck / `tsc` build successfully.

## Notes

Packages: `packages/embed-vue`, `embed-react`, `embed-svelte`, `embed-web-component`. Build helper: `packages/build-embed-sdks.sh`.
