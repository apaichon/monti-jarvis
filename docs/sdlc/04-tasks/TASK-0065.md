---
id: TASK-0065
title: "Tenant portal /tenant/embed config UI"
status: completed
sprint: SPRINT-014
points: 3
owner: dev
updated: 2026-07-12
---

# TASK-0065: Tenant Embed Admin UI

## Outcome

Tenant admins manage embed from `/tenant/embed`.

## Acceptance Criteria

1. Route `/tenant/embed` (requires tenant session / active where other tenant routes do).
2. Shows: enabled toggle, embed_key (copy), allowed origins (list edit), default agent (optional select), **Copy snippet** button.
3. Rotate key with confirm → new key; UI updates snippet.
4. Save origins → PUT API; validation for origin format (`https://example.com`).
5. Nav link in tenant shell.
6. EN primary labels; short help text for allowlist empty = allow all (dev warning).

## Verification

```bash
open http://localhost:8091/tenant/embed
# Login tenant_admin → configure → paste snippet into test page
```

## Done notes (2026-07-11)

- `/tenant/embed` — enable, origins, default agent, copy snippet, rotate key
- Nav **Embed** link in tenant shell
- API client `apps/tenant-web/src/lib/api/embed.ts`
