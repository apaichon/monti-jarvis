---
id: TASK-0013
title: "Tenant resolution in KM/calls; AUTH_DISABLED dev bypass"
status: todo
sprint: SPRINT-003
points: 3
owner: dev
updated: 2026-07-07
---

# TASK-0013: Tenant Resolution in KM/Calls; AUTH_DISABLED Dev Bypass

## Outcome

KM and call services resolve `tenant_id` from auth context; dev bypass preserves Sprint 1–2 behavior; docs updated.

## Acceptance Criteria

1. `internal/km` and `internal/rag` receive tenant from auth context, not spoofable `X-Tenant-Id` when `AUTH_DISABLED=false`.
2. `X-Tenant-Id` honored only when `AUTH_DISABLED=true` (backward compatible dev).
3. Call session `tenant_id` set from auth context on `POST /api/calls` when auth enabled.
4. `AGENTS.md`, `LOCAL-DEV.md`, and `api-spec.md` document auth env vars and curl login flow.
5. Regression: `AUTH_DISABLED=true` — `make km-seed`, voice call, and chat unchanged from v0.3.0.

## Verification

```bash
AUTH_DISABLED=true make km-seed && go test ./...
AUTH_DISABLED=false make restart
# tenant_admin login → km-seed scoped to demo tenant only
```