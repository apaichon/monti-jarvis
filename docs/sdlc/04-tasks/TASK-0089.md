---
id: TASK-0089
title: "Tenant customer management and CSV import UI"
status: completed
sprint: SPRINT-019
feature: FEAT-0021
points: 4
owner: dev
updated: 2026-07-12
---

# TASK-0089: Tenant Customers UI

## Outcome

Responsive `/tenant/customers` page for customer search, create/edit/deactivate, CSV import, and domain rules.

## Acceptance criteria

1. Add Customers navigation and `/tenant/customers` route using the Monti design system.
2. List supports query/status/tier filters and clear empty/loading/error states.
3. Create/edit surface assigns one tier and zero or more groups.
4. Import flow requires dry-run preview before commit and clearly shows accepted/rejected counts plus row errors.
5. Domain Rules tab manages domain, policy, default tier/group, and active state.
6. Mobile layout remains usable and Thai/English-ready copy avoids hard-coded locale assumptions.

## Verification

```bash
cd apps/tenant-web && npm run check && npm run build
```

## Implementation evidence

- Added `apps/tenant-web/src/routes/customers/+page.svelte` and the typed customer API client.
- Added Customers navigation, responsive directory/editor, import workflow, row errors, and domain-rule tab.
- Commit remains disabled until the selected file completes dry-run validation.

## Review — PASS

The Svelte implementation follows UX T12 and preserves existing session/API patterns. `svelte-check` reports 0 errors and 0 warnings; the tenant production build passes.
