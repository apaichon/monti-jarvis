---
id: TASK-0010
title: "Postgres tenants, users, roles schema + dev seed"
status: todo
sprint: SPRINT-003
points: 3
owner: devops
updated: 2026-07-07
---

# TASK-0010: Postgres Tenants, Users, Roles Schema + Dev Seed

## Outcome

Auth tables exist in `callcenter` schema; dev seed creates `demo` tenant plus platform and tenant-admin users for local testing.

## Acceptance Criteria

1. `infra-init.sh` creates `tenants`, `users`, `user_roles`, `refresh_tokens` tables with indexes on `users(email)` and `refresh_tokens(token_hash)`.
2. `tenants` row for `demo` (id aligns with `DEMO_TENANT_ID`); slug `demo`, status `active`.
3. Seed users: `platform@monti.local` (`platform_admin`), `admin@demo.local` (`tenant_admin`, tenant `demo`); passwords documented in `LOCAL-DEV.md` (dev-only defaults).
4. Migration is idempotent (`CREATE TABLE IF NOT EXISTS`, seed uses `ON CONFLICT`).
5. `er-diagram.md` updated with auth entities and relationships.

## Verification

```bash
make infra-init
psql "$POSTGRES_URL" -c "SELECT email, role FROM callcenter.users u JOIN callcenter.user_roles r ON ..."
```