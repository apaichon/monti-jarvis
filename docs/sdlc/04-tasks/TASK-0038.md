---
id: TASK-0038
title: "Platform admin UI — payment gateway settings"
status: todo
sprint: SPRINT-008
points: 3
owner: dev
updated: 2026-07-09
---

# TASK-0038: Platform Admin Payment Settings UI

## Outcome

Platform operators configure the payment gateway and test connectivity from `/admin/settings/payment`.

## Acceptance Criteria

1. Route `/admin/settings/payment` — provider select (`stripe`|`mock`), mode, publishable key, secret key (password input), webhook secret (password input).
2. **Save** calls `PUT /api/platform/payment-gateway`; **Test connection** calls `POST .../test`.
3. Uses shared `feedback.success()` / `feedback.error()` dialog pattern (Sprint 6–7).
4. Nav link in platform admin shell (Settings → Payment or sidebar item).
5. Shows masked secret tail and last connection status from `GET` on load.

## Verification

```bash
make platform-admin-web && make restart
open http://localhost:8091/admin/settings/payment
# Select mock → Save → Test connection → success dialog
```