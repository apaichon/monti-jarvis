---
id: TASK-0080
title: "Scenario checklist + embed preview link"
status: done
sprint: SPRINT-017
points: 3
owner: dev
updated: 2026-07-12
---

# TASK-0080: Scenarios + Embed Link

## Outcome

Help tenants validate KM coverage and open their embed surface from Preview.

## Acceptance Criteria

1. Static **suggested questions** by topic (e.g. general greeting, billing invoice, technical reset) — click fills input.  
2. When embed enabled: button **Open embed preview** opens `/embed?key=…&parent_origin=…` or tenant public base with key (same-origin OK).  
3. When embed disabled: CTA link to `/tenant/embed` to enable.  
4. Optional: show last preview session id / “connected as tenant X” debug line for support.  
5. No new backend required beyond embed GET config already shipped.

## Verification

```bash
# UI: click scenario → message prefilled → send
# Embed enabled → new tab shows Monti embed for tenant
```
