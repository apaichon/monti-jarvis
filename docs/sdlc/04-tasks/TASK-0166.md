---
id: TASK-0166
title: "Idempotent usage ledger and reconciliation runs"
status: completed
sprint: SPRINT-045
feature: FEAT-0039
design: DES-0042
points: 3
owner: devops/dev
updated: 2026-07-25
---

# TASK-0166: Idempotent Usage Ledger and Reconciliation Runs

## Outcome

Call, mobile, KM, avatar, storage, and concurrency lifecycle events persist as
tenant-scoped logical usage events and can be replayed through bounded,
operator-controlled reconciliation runs without double counting.

## Acceptance criteria

1. Usage events are unique by `(tenant_id, idempotency_key)` and duplicate
   delivery returns the original logical result.
2. Events retain dimension, unit, amount, period, source, and entitlement
   snapshot context without storing raw provider bodies or credentials.
3. Failed starts, disconnects, timeouts, deletes, and retries do not create
   duplicate consumption; correction states are explicit.
4. Reconciliation runs validate bounded date ranges, expose source watermarks,
   mismatch/correction counts, and safe error codes.
5. Two tenants can replay the same fixture independently without cross-tenant
   reads or counter mutation.

## Verification

Postgres/Redis/MinIO/ClickHouse fixture tests, replay tests, and reconciliation
API contract tests must pass. Docker-backed UAT is required before final S45
close.

## Implementation completed

- Added tenant-scoped Postgres `usage_events` with unique idempotency keys,
  explicit correction states, source metadata, and entitlement snapshot context.
- Added bounded, idempotent reconciliation runs with safe status/error codes.
- Wired call and mobile-call finalization to the ledger as a best-effort,
  duplicate-safe write.
- Reconciliation watermarks now report applied, correction, reversed, and
  voided event counts; correction totals are persisted on the run.
- Entitlement snapshot IDs are retained on call and mobile usage events.
- Local Docker fixture replay verified one logical event per tenant/idempotency
  key, bounded reconciliation completion, duplicate replay, and safe cleanup.

## Verification evidence

- `go test ./...` passed: 209 tests in 33 packages.
- `go vet ./...`, `go build ./cmd/server`, `make build`, migration, and
  `git diff --check` passed.
- Docker-backed UAT passed for Postgres, Redis DB 4, MinIO, ClickHouse,
  tenant isolation, reconciliation RBAC, duplicate replay, and outage states.

## Review — PASS

- The ledger remains append-only and tenant-scoped with unique idempotency
  keys; reconciliation is bounded and does not store provider payloads.
- Correction metadata is explicit and safe error behavior is preserved.
