---
id: TASK-0077
title: "Preview session flag + Redis concurrent preview keys"
status: done
sprint: SPRINT-017
points: 3
owner: devops
updated: 2026-07-12
---

# TASK-0077: Preview Session Infrastructure

## Outcome

Lightweight way to mark preview traffic and cap concurrent preview voice sessions per tenant.

## Acceptance Criteria

1. Call sessions (or chat metadata) can record `source` / `mode` = `preview` when created from tenant preview APIs (reuse `call_sessions` with optional column **or** Redis/session field documented if no migration preferred).  
2. Redis key design: `{prefix}preview:concurrent:{tenant}` with short TTL for soft concurrent cap (default **2**).  
3. Env (optional): `PREVIEW_MAX_CONCURRENT` default 2; document in LOCAL-DEV.  
4. No change to production embed/customer paths.  
5. Unit test for concurrent acquire/release helper (or extend `internal/quota`).

## Verification

```bash
# After implement: redis-cli KEYS 'monti_jarvis:preview:*'
go test ./internal/quota/ -count=1
```
