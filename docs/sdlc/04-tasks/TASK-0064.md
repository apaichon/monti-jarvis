---
id: TASK-0064
title: "Embed loader JS + customer embed surface"
status: completed
sprint: SPRINT-014
points: 4
owner: dev
updated: 2026-07-12
---

# TASK-0064: Loader + Embed UI

## Outcome

Static loader snippet mounts Monti iframe; customer embed route runs chat (and voice when available) for resolved tenant.

## Acceptance Criteria

1. Static asset e.g. `GET /embed/monti-embed.js` (or under customer-web static).
2. Snippet API:
   ```html
   <script src="https://{host}/embed/monti-embed.js"
     data-embed-key="KEY"
     data-position="bottom-right"
     async></script>
   ```
3. Loader creates floating button + iframe → `/embed?key=KEY` (or `/e/{key}`).
4. Embed page (customer-web): compact layout, load config via public API, set tenant for chat/voice (`X-Tenant-Id` or query handled by API resolve).
5. Workforce list + text chat works in iframe for that tenant.
6. Full portal `/` demo path unchanged.
7. CSP / `X-Frame-Options` compatible with allowlist (prefer CSP `frame-ancestors`).

## Verification

```bash
# Open docs/fixtures/embed-demo.html or local static HTML with snippet
# Chat a message as embed tenant
```

## Done notes (2026-07-11)

- `static/embed/monti-embed.js` — floating launcher + iframe → `/embed?key=`
- Customer route `/embed` — compact chat, public resolve, `X-Tenant-Id`
- CSP `frame-ancestors *` on `/embed*` responses
- Fixture: `docs/fixtures/embed-demo.html`
