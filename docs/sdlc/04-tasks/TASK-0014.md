---
id: TASK-0014
title: "Postgres packages schema + dev seeds + provider catalog stub"
status: todo
sprint: SPRINT-004
points: 3
owner: devops
updated: 2026-07-07
---

# TASK-0014: Postgres Packages Schema + Dev Seeds + Provider Catalog Stub

## Outcome

Commercial package tables exist in `callcenter`; dev seed creates Starter/Pro/Enterprise packages and assigns `demo` → Starter. Provider catalog stub rows seeded per ER Sprint 3–4 note.

## Acceptance Criteria

1. `infra-init.sh` (or migration script) creates `package_rule_schemas`, `packages`, `package_limits` (`rules` jsonb + `rules_schema_id` FK), `tenant_entitlements` (`rules_snapshot`, `rules_schema_id`) with audit columns and partial unique index for one active entitlement per tenant.
2. Seed `rules-v1` in `package_rule_schemas` with `fields` jsonb per [08-packages-spec.md](../02-design/08-packages-spec.md) §2.
3. Seed packages: `pkg-starter`, `pkg-pro`, `pkg-enterprise` with `rules` jsonb per §7.
4. Seed active entitlement: tenant `demo` → `pkg-starter` with `rules_snapshot` copy.
5. Idempotent DDL + seeds (`CREATE TABLE IF NOT EXISTS`, `ON CONFLICT`).
6. Stub seeds for `embedding_models` (`emb-gemini-001`) and `voice_providers` (`voice-gemini-live`) when tables exist.
7. `03-er-diagram.md` updated with package entities and relationships.

## Verification

```bash
make infra-init
psql "$POSTGRES_URL" -c "SELECT slug, status FROM callcenter.packages ORDER BY slug"
psql "$POSTGRES_URL" -c "SELECT tenant_id, package_id, status FROM callcenter.tenant_entitlements"
```