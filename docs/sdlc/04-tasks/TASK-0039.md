---
id: TASK-0039
title: "Infra status, env docs, E2E smoke"
status: todo
sprint: SPRINT-008
points: 2
owner: dev
updated: 2026-07-09
---

# TASK-0039: Infra Status + Docs + E2E

## Outcome

Operators see payment gateway readiness on `/api/infra`; local dev docs list ChillPay env vars; Playwright smoke covers settings page.

## Acceptance Criteria

1. `GET /api/infra` includes `payment_gateway: {configured, provider, mode}`.
2. `/healthz` sprint string → `SPRINT-008` when shipped.
3. `docs/sdlc/07-deployment/LOCAL-DEV.md` documents `CHILLPAY_MERCHANT_CODE`, `CHILLPAY_API_KEY`, `CHILLPAY_MD5_KEY`, `CHILLPAY_BASE_URL`, `CHILLPAY_ROUTE_NO`, `CHILLPAY_CURRENCY`, `CHILLPAY_RETURN_URL`, `PAYMENT_CALLBACK_DEV_BYPASS`.
4. E2E `e2e/tests/platform-payment-gateway.spec.ts` — platform login → open payment settings → mock test connection.

## Verification

```bash
curl http://localhost:8091/api/infra | jq .payment_gateway
cd e2e && npx playwright test platform-payment-gateway.spec.ts
```